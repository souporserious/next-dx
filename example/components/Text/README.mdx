<Summary>
  `Text` is a small abstraction which makes it easy to use predefined text
  variants. It also accepts props which make it easy to access theme color
  variables, truncation, and line-clamping.
</Summary>
